def take_while(l, predicate):
    """
    pre: `l` est une `List`
    pre: `predicate` est une fonction booléenne
    post: retourne une nouvelle liste contenant les premiers éléments qui vérifient `predicate`
    """
    pass

# Exemples :
# take_while([1[2[3[4[]]]]], lambda x: x < 3) → [1[2[]]]
# take_while([1[2[3[]]]], lambda x: x < 0) → []
