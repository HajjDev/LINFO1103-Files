from Tree import BinaryTree

def all_leaf_values(tree: BinaryTree) -> list:
    """
    pre: tree est un BinaryTree
    post: retourne une liste contenant les valeurs de toutes les feuilles
    """

# Exemple :
# arbre = BinaryTree(1)
# arbre.left().elem = 2
# arbre.right().elem = 3
# all_leaf_values(arbre) → [2, 3]